{
  "name": "warteg-pos-mvp",
  "version": "1.0.0",
  "description": "Minimum Viable Product (MVP) of Warteg POS Application with integrated frontend & backend",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && npm.cmd install --prefix warteg-backend && npm.cmd install --prefix warteg-frontend",
    "dev:backend": "npm.cmd run dev --prefix warteg-backend",
    "dev:frontend": "npm.cmd run dev --prefix warteg-frontend",
    "dev": "concurrently \"npm.cmd run dev:backend\" \"npm.cmd run dev:frontend\"",
    "build": "npm.cmd run build --prefix warteg-frontend"
  },
  "keywords": [
    "warteg",
    "pos",
    "mvp"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
